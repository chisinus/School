
@{
    ViewBag.Title = "Home";
    Layout = "~/Views/Shared/_LayoutSportsStore.cshtml";
}

<div class="navbar navbar-inverse">
    <a class="navbar-brand" href="#">SPORTS STORE</a>
</div>

<div class="panel panel-default row" ng-controller="productListCtrl">
    <div class="col-xs-3">
        <a ng-click="selectCategory()" class="btn btn-block btn-default btn-lg">Home</a>
        <a ng-repeat="product in products | orderBy : 'Category' | unique : 'Category'"
           ng-click="selectCategory(product)" class="btn btn-block btn-default btn-lg" ng-class="getCategoryClass(product)">
            {{product}}
        </a>
    </div>
    <div class="col-xs-8">
        <div class="well" 
             ng-repeat="product in products | filter : categoryFilter | range:selectedPage:pageSize">
            <h3>
                <strong>{{product.Name}}</strong>
                <span class="pull-right label label-primary">
                    {{product.Price | currency}}
                </span>
            </h3>
            <span class="lead">{{product.Description}}</span>
        </div>
        <div class="pull-right btn-group">
            <a ng-repeat="page in products | filter : categoryFilter | pageCount: pageSize" 
               ng-click="selectPage($index+1)" 
               class="btn btn-default" ng-class="getPageClass($index+1)">
                {{$index+1}}
            </a>
        </div>
    </div>
</div>